<resources>
    <!-- Base application theme. -->
    <style name="Theme.BystanderApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and destination theme. -->
        <item name="windowSplashScreenBackground">#F0F4F8</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo_bystander</item>
        <item name="postSplashScreenTheme">@style/Theme.BystanderApp</item>
    </style>
</resources>
