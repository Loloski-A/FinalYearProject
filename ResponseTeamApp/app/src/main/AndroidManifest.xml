<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ResponseTeamApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Add your Google Maps API Key here -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_API_KEY" />

        <activity
            android:name=".TeamLoginActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting.Team">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TeamRegisterActivity" android:exported="false" />
        <activity android:name=".TeamDashboardActivity" android:exported="false" />
        <activity android:name=".TeamIncidentDetailActivity" android:exported="false" />
        <activity android:name=".TeamProfileActivity" android:exported="false" />
        <activity android:name=".TeamMapActivity" android:exported="false" />
        <activity android:name=".TeamNotificationsActivity" android:exported="false" />

        <!-- MainActivity can be removed if not used, or kept as a splash screen launcher -->
        <activity android:name=".MainActivity" android:exported="false" />

    </application>

</manifest>
