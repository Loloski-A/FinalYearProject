<resources>
    <!-- Base application theme. -->
    <style name="Theme.ResponseTeamApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.App.Starting.Team" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#1A237E</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo_team</item>
        <item name="postSplashScreenTheme">@style/Theme.ResponseTeamApp</item>
    </style>
</resources>